{
  "mcpServers": {
    "database": {
      "type": "stdio",
      "command": "node",
      "args": ["server.js"],
      "env": {
        "NODE_ENV": "production",
        "DATABASE_TOKEN": "example-token"
      }
    }
  }
}
